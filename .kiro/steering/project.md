# 爪印留聲專案規則

以下規則在本專案的所有開發階段持續生效：

1. 一次只執行使用者指定的階段，不提前實作後續功能。
2. 公網可用和跨裝置資料優先於動畫。
3. 不虛構 API、模型結果、素材或測試通過。
4. 不把 API 密鑰寫入程式碼、文件或日誌。
5. 每階段結束時，說明執行方法、驗收動作、實際驗證結果和未完成事項。
6. 每階段提醒使用者保存完整 AI 對話，並保存一個可回退的 Git commit；提交前檢查不包含密鑰、環境檔和不必要的大型媒體檔案。
